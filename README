App
=================================

for the app, just run

```
activator
[app] ~run
```

for the app UI, just go to `app-ui` and run

```
npm install
``` 

then, for dev run

```
npm run dev
```

for build run 

```
npm run build
```